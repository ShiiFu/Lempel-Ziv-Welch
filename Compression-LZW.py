#-*- coding:UTF-8 -*- 
#Python 2.7 a ete utilise 







 w = Nul;
   for c in file:
       if(w + c existe dans le dictionnaire) in list():
           w = w + c;
       else
           ajouter w + c au dictionnaire;
           écrire le code de w;
           w = c;
       fin si
   fin tant que
   écrire le code de w;